#ifndef _MACRO_H_
#define _MACRO_H_

void deploy(int distance, int vel);
void move_tray(int distance, int vel);
void reset_motors();
void intake(int vel);
void move_intake(int distance, int vel);
void move_lift(int distnace, int vel);

#endif
